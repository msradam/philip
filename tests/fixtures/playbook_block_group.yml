- name: block group
  hosts: localhost
  gather_facts: no
  tasks:
    - name: wrap
      block:
        - name: first inner
          ansible.builtin.ping:
        - name: second inner
          ansible.builtin.ping:
